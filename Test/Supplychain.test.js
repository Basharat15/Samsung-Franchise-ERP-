const supply = require("./supplychain");
test("if budget amount is lower than ordered product's price, then don't purchase product", () => {
    expect(supply("updateproduct")).toBe("updateproduct();");
});